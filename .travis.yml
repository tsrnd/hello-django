dist: xenial
language: python
python:
  - '3.7.1'
services:
  - docker
cache:
  directories:
    - $HOME/.docker/images/
before_install:
  - cp .env.example .env
  - make load-images
install:
  - make up-data
script:
  - timeout 120s make test
before_cache:
  - make save-images
